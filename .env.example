# Environment variables required for local development
# Copy to .env.local and fill in real values.

# NextAuth secret for JWT/session encryption. Generate with: openssl rand -hex 32
NEXTAUTH_SECRET=

# Base URL of your app (used by NextAuth callbacks)
AUTH_URL=http://localhost:3000

# Postgres connection string. Example (Vercel Postgres):
# postgres://username:password@host:5432/database
POSTGRES_URL=

# Optional: Non-pooling URL if using serverless functions differently
# POSTGRES_URL_NON_POOLING=

# Any additional custom variables can be added below:
# CUSTOM_VAR=
